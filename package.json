{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./apps/**\" --filter \"./packages/shared/**\" --filter \"./scripts\" --if-present run typecheck",
    "db:migrate": "pnpm --filter @workspace/db run push"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "optionalDependencies": {
    "@esbuild/darwin-arm64": "0.27.3",
    "@rollup/rollup-darwin-arm64": "^4.60.3"
  }
}
